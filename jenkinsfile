pipeline {
    agent {label "SPC" }
    stages {
        stage('git checkout') {
            steps {
                git url : 'https://github.com/DeepthiReddy7/spring-petclinic.git',
                branch : 'main'
            }
        }
        stage('build and scan'){
            steps {
                sh 'mv package sonar:sonar'
            }
        }
    }
}